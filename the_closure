我对闭包的理解就是 函数中的函数。

在JavaScript中（ES6 中块级作用域除外）只有全局作用域和函数作用域。
如果在函数外部想访问函数内部的变量是不可能的。

比如：

    function fun(){
     var x=10;
     alert(x);
}
   如果想在函数外面访问 x ，则访问到的是全局变量x ，如果不存在全局变量x ，则会报错x is not defined。
